═══════════════════════════════════════════════════════════════════
              🚀 DEPLOY ECHOCHAT TO VERCEL
═══════════════════════════════════════════════════════════════════

⚠️ IMPORTANT: EchoChat uses WebSockets (Socket.io) which requires
a separate backend server. Vercel is great for the frontend, but
you'll need a different service for the backend.

RECOMMENDED DEPLOYMENT STRATEGY:
- Frontend (Client): Vercel ✅
- Backend (Server): Railway, Render, or Heroku ✅


═══════════════════════════════════════════════════════════════════
📋 OPTION 1: DEPLOY FRONTEND TO VERCEL (RECOMMENDED)
═══════════════════════════════════════════════════════════════════

STEP 1: Prepare the Client
───────────────────────────────────────────────────────────────────

1. Update API URL in client code:
   - Open: client/src/lib/api.ts
   - Change: http://localhost:3001
   - To: https://your-backend-url.railway.app

2. Update Socket URL:
   - Open: client/src/lib/socket.ts
   - Change: http://localhost:3001
   - To: https://your-backend-url.railway.app


STEP 2: Deploy Client to Vercel
───────────────────────────────────────────────────────────────────

1. Install Vercel CLI:
   npm install -g vercel

2. Navigate to client folder:
   cd "Discord alternative (Beta)/client"

3. Login to Vercel:
   vercel login

4. Deploy:
   vercel

5. Follow prompts:
   - Set up and deploy? Yes
   - Which scope? Your account
   - Link to existing project? No
   - Project name? echochat
   - Directory? ./
   - Override settings? No

6. Production deployment:
   vercel --prod

7. Your frontend is now live at:
   https://echochat.vercel.app


STEP 3: Deploy Backend to Railway
───────────────────────────────────────────────────────────────────

1. Go to: https://railway.app

2. Sign up with GitHub

3. Click "New Project"

4. Select "Deploy from GitHub repo"

5. Connect your GitHub account

6. Push your code to GitHub:
   cd "Discord alternative (Beta)"
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main

7. In Railway:
   - Select your repository
   - Select "server" folder as root directory
   - Railway will auto-detect Node.js

8. Add Environment Variables in Railway:
   - Click "Variables" tab
   - Add all variables from server/.env:
     * PORT=3001
     * NODE_ENV=production
     * CORS_ORIGIN=https://echochat.vercel.app
     * FIREBASE_PROJECT_ID=studiowl-3b22d
     * FIREBASE_PRIVATE_KEY=(your key)
     * FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@studiowl-3b22d.iam.gserviceaccount.com
     * SMTP_HOST=smtp.gmail.com
     * SMTP_PORT=587
     * SMTP_SECURE=false
     * SMTP_USER=juusojuusto112@gmail.com
     * SMTP_PASS=zwlcbmlrxsrkutme
     * SMTP_FROM_NAME=EchoChat
     * SMTP_FROM_EMAIL=juusojuusto112@gmail.com

9. Deploy:
   - Railway will automatically deploy
   - Get your backend URL: https://echochat-production.up.railway.app

10. Update Frontend URLs:
    - Go back to your client code
    - Update API and Socket URLs to Railway URL
    - Redeploy to Vercel: vercel --prod


═══════════════════════════════════════════════════════════════════
📋 OPTION 2: DEPLOY BACKEND TO RENDER
═══════════════════════════════════════════════════════════════════

1. Go to: https://render.com

2. Sign up with GitHub

3. Click "New +" → "Web Service"

4. Connect your GitHub repository

5. Configure:
   - Name: echochat-backend
   - Root Directory: server
   - Environment: Node
   - Build Command: npm install && npm run build
   - Start Command: npm start

6. Add Environment Variables (same as Railway above)

7. Deploy

8. Get your URL: https://echochat-backend.onrender.com

9. Update frontend URLs and redeploy


═══════════════════════════════════════════════════════════════════
📋 OPTION 3: DEPLOY BOTH TO SAME SERVICE (NOT RECOMMENDED)
═══════════════════════════════════════════════════════════════════

You can deploy both to Railway or Render, but it's more complex.
The recommended approach is:
- Frontend on Vercel (fast, free, optimized for React)
- Backend on Railway/Render (supports WebSockets)


═══════════════════════════════════════════════════════════════════
🔧 UPDATE FIREBASE SETTINGS FOR PRODUCTION
═══════════════════════════════════════════════════════════════════

1. Add Production Domain to Firebase:
   - Go to: https://console.firebase.google.com/project/studiowl-3b22d/authentication/settings
   - Scroll to "Authorized domains"
   - Click "Add domain"
   - Add: echochat.vercel.app
   - Add: your-backend-url.railway.app

2. Update Google Sign-In:
   - Go to: https://console.firebase.google.com/project/studiowl-3b22d/authentication/providers
   - Click "Google"
   - Add authorized domains if needed

3. Update Firestore Rules (IMPORTANT!):
   - Go to: https://console.firebase.google.com/project/studiowl-3b22d/firestore/rules
   - Replace with production rules:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isEmailVerified() {
      return request.auth.token.email_verified == true;
    }
    
    match /users/{userId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && request.auth.uid == userId;
    }
    
    match /servers/{serverId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && isEmailVerified();
      allow update, delete: if isSignedIn();
      
      match /{document=**} {
        allow read, write: if isSignedIn();
      }
    }
    
    match /messages/{messageId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && isEmailVerified();
      allow update, delete: if isSignedIn() && 
        resource.data.authorId == request.auth.uid;
    }
  }
}


═══════════════════════════════════════════════════════════════════
✅ DEPLOYMENT CHECKLIST
═══════════════════════════════════════════════════════════════════

Before Deployment:
[ ] Firebase authentication enabled
[ ] Firestore database created
[ ] All environment variables ready
[ ] GitHub repository created

Frontend (Vercel):
[ ] Client deployed to Vercel
[ ] Custom domain configured (optional)
[ ] Environment variables set
[ ] API URLs updated to production backend

Backend (Railway/Render):
[ ] Server deployed to Railway/Render
[ ] Environment variables configured
[ ] CORS_ORIGIN set to Vercel URL
[ ] WebSocket connections working

Firebase:
[ ] Production domains added to authorized domains
[ ] Firestore rules updated to production mode
[ ] Google Sign-In configured for production
[ ] Email templates customized

Testing:
[ ] Registration works
[ ] Email verification works
[ ] Login works (email/username/phone)
[ ] Google Sign-In works
[ ] Messages send in real-time
[ ] Servers and channels work


═══════════════════════════════════════════════════════════════════
💰 COST BREAKDOWN
═══════════════════════════════════════════════════════════════════

Vercel (Frontend):
- Free tier: Perfect for EchoChat
- Unlimited bandwidth
- Automatic HTTPS
- Global CDN

Railway (Backend):
- Free tier: $5 credit/month
- Enough for development/testing
- Paid: $5/month for hobby plan

Render (Backend Alternative):
- Free tier: Available
- Spins down after inactivity
- Paid: $7/month for always-on

Firebase:
- Free tier: Generous limits
- Authentication: Free
- Firestore: 50K reads/day free
- Enough for small-medium apps

Gmail SMTP:
- Free: 500 emails/day
- Enough for most apps

TOTAL COST:
- Development: $0/month (all free tiers)
- Production: $5-7/month (backend hosting)


═══════════════════════════════════════════════════════════════════
🎉 YOU'RE LIVE!
═══════════════════════════════════════════════════════════════════

Your EchoChat is now deployed and accessible worldwide!

Frontend: https://echochat.vercel.app
Backend: https://your-backend.railway.app

Share the link and start building your community!
